<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nixer.nprox.dao.AgentUserNodesNumDao">

    <resultMap type="com.nixer.nprox.entity.AgentUserNodesNum" id="AgentUserNodesNumMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userid" column="userid" jdbcType="INTEGER"/>
        <result property="agentid" column="agentid" jdbcType="INTEGER"/>
        <result property="node_num" column="node_num" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="AgentUserNodesNumMap">
        select id,
               userid,
               agentid,
               node_num
        from nprox.agent_user_nodes_num
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="AgentUserNodesNumMap">
        select id,
               userid,
               agentid,
               node_num
        from nprox.agent_user_nodes_num limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="AgentUserNodesNumMap">
        select
        id, userid, agentid, node_num
        from nprox.agent_user_nodes_num
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userid != null">
                and userid = #{userid}
            </if>
            <if test="agentid != null">
                and agentid = #{agentid}
            </if>
            <if test="nodeNum != null">
                and node_num = #{node_num}
            </if>
        </where>
    </select>
    <select id="findByUserid" resultType="com.nixer.nprox.entity.AgentUserNodesNum">
        select * from agent_user_nodes_num  where  agentid=#{agentid} and userid=#{userid}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into nprox.agent_user_nodes_num(userid, agentid, node_num)
        values (#{userid}, #{agentid}, #{node_num})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into nprox.agent_user_nodes_num(userid, agentid, node_num)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userid}, #{entity.agentid}, #{entity.node_num})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into nprox.agent_user_nodes_num(userid, agentid, node_num)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userid}, #{entity.agentid}, #{entity.node_num})
        </foreach>
        on duplicate key update
        userid = values(userid) , agentid = values(agentid) , node_num = values(node_num)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update nprox.agent_user_nodes_num
        <set>
            <if test="userid != null">
                userid = #{userid},
            </if>
            <if test="agentid != null">
                agentid = #{agentid},
            </if>
            <if test="node_num != null">
                node_num = #{node_num},
            </if>
        </set>
        where id = #{id}
    </update>
    <update id="updateAgentUserNodesNum">
        update agent_user_nodes_num set node_num=node_num+#{updatenodes}
        where agentid = #{agentid} and userid =#{userid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from nprox.agent_user_nodes_num
        where id = #{id}
    </delete>

</mapper>

