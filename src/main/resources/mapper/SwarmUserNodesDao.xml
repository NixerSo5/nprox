<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nixer.nprox.dao.SwarmUserNodesDao">

    <resultMap type="com.nixer.nprox.entity.SwarmUserNodes" id="SwarmUserNodesMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userid" column="userid" jdbcType="INTEGER"/>
        <result property="nodeUid" column="node_uid" jdbcType="VARCHAR"/>
        <result property="overtime" column="overtime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SwarmUserNodesMap">
        select id,
               userid,
               node_uid,
               overtime
        from nprox.swarm_user_nodes
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SwarmUserNodesMap">
        select id,
               userid,
               node_uid,
               overtime
        from nprox.swarm_user_nodes limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="SwarmUserNodesMap">
        select
        id, userid, node_uid, overtime
        from nprox.swarm_user_nodes
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userid != null">
                and userid = #{userid}
            </if>
            <if test="nodeUid != null and nodeUid != ''">
                and node_uid = #{nodeUid}
            </if>
            <if test="overtime != null">
                and overtime = #{overtime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into nprox.swarm_user_nodes(userid, node_uid, overtime)
        values (#{userid}, #{nodeUid}, #{overtime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into nprox.swarm_user_nodes(userid, node_uid, overtime)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userid}, #{entity.nodeUid}, #{entity.overtime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into nprox.swarm_user_nodes(userid, node_uid, overtime)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userid}, #{entity.nodeUid}, #{entity.overtime})
        </foreach>
        on duplicate key update
        userid = values(userid) , node_uid = values(node_uid) , overtime = values(overtime)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update nprox.swarm_user_nodes
        <set>
            <if test="userid != null">
                userid = #{userid},
            </if>
            <if test="nodeUid != null and nodeUid != ''">
                node_uid = #{nodeUid},
            </if>
            <if test="overtime != null">
                overtime = #{overtime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from nprox.swarm_user_nodes
        where id = #{id}
    </delete>
    <delete id="deleteUserNodes">
        DELETE
        FROM
        swarm_user_nodes
        WHERE
        userid = #{userid}
        AND node_uid IN
        <foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
            #{item.node_uid}
        </foreach>
    </delete>

</mapper>

